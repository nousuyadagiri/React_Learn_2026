# Hooks
--> Hooks are special functions that allow function components to use React features such as state, lifecycle methods, context, refs, performance optimizations, and server actions without writing class components.
